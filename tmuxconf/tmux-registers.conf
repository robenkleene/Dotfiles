bind-key -T copy-mode-vi '"' switch-client -T copy-mode-vi-quote
bind-key '"' switch-client -T quote

bind-key -T copy-mode-vi-quote a switch-client -T copy-mode-vi-quote-a
bind-key -T copy-mode-vi-quote-a y send-keys -X pipe "tmux loadb -b a -"
bind-key -T quote a switch-client -T quote-a
bind-key -T quote-a y paste-buffer -p -b a

bind-key -T copy-mode-vi-quote b switch-client -T copy-mode-vi-quote-b
bind-key -T copy-mode-vi-quote-b y send-keys -X pipe "tmux loadb -b b -"
bind-key -T quote b switch-client -T quote-b
bind-key -T quote-b y paste-buffer -p -b b

bind-key -T copy-mode-vi-quote c switch-client -T copy-mode-vi-quote-c
bind-key -T copy-mode-vi-quote-c y send-keys -X pipe "tmux loadb -b c -"
bind-key -T quote c switch-client -T quote-c
bind-key -T quote-c y paste-buffer -p -b c

bind-key -T copy-mode-vi-quote d switch-client -T copy-mode-vi-quote-d
bind-key -T copy-mode-vi-quote-d y send-keys -X pipe "tmux loadb -b d -"
bind-key -T quote d switch-client -T quote-d
bind-key -T quote-d y paste-buffer -p -b d

bind-key -T copy-mode-vi-quote e switch-client -T copy-mode-vi-quote-e
bind-key -T copy-mode-vi-quote-e y send-keys -X pipe "tmux loadb -b e -"
bind-key -T quote e switch-client -T quote-e
bind-key -T quote-e y paste-buffer -p -b e

bind-key -T copy-mode-vi-quote f switch-client -T copy-mode-vi-quote-f
bind-key -T copy-mode-vi-quote-f y send-keys -X pipe "tmux loadb -b f -"
bind-key -T quote f switch-client -T quote-f
bind-key -T quote-f y paste-buffer -p -b f

bind-key -T copy-mode-vi-quote g switch-client -T copy-mode-vi-quote-g
bind-key -T copy-mode-vi-quote-g y send-keys -X pipe "tmux loadb -b g -"
bind-key -T quote g switch-client -T quote-g
bind-key -T quote-g y paste-buffer -p -b g

bind-key -T copy-mode-vi-quote h switch-client -T copy-mode-vi-quote-h
bind-key -T copy-mode-vi-quote-h y send-keys -X pipe "tmux loadb -b h -"
bind-key -T quote h switch-client -T quote-h
bind-key -T quote-h y paste-buffer -p -b h

bind-key -T copy-mode-vi-quote i switch-client -T copy-mode-vi-quote-i
bind-key -T copy-mode-vi-quote-i y send-keys -X pipe "tmux loadb -b i -"
bind-key -T quote i switch-client -T quote-i
bind-key -T quote-i y paste-buffer -p -b i

bind-key -T copy-mode-vi-quote j switch-client -T copy-mode-vi-quote-j
bind-key -T copy-mode-vi-quote-j y send-keys -X pipe "tmux loadb -b j -"
bind-key -T quote j switch-client -T quote-j
bind-key -T quote-j y paste-buffer -p -b j

bind-key -T copy-mode-vi-quote k switch-client -T copy-mode-vi-quote-k
bind-key -T copy-mode-vi-quote-k y send-keys -X pipe "tmux loadb -b k -"
bind-key -T quote k switch-client -T quote-k
bind-key -T quote-k y paste-buffer -p -b k

bind-key -T copy-mode-vi-quote l switch-client -T copy-mode-vi-quote-l
bind-key -T copy-mode-vi-quote-l y send-keys -X pipe "tmux loadb -b l -"
bind-key -T quote l switch-client -T quote-l
bind-key -T quote-l y paste-buffer -p -b l

bind-key -T copy-mode-vi-quote m switch-client -T copy-mode-vi-quote-m
bind-key -T copy-mode-vi-quote-m y send-keys -X pipe "tmux loadb -b m -"
bind-key -T quote m switch-client -T quote-m
bind-key -T quote-m y paste-buffer -p -b m

bind-key -T copy-mode-vi-quote n switch-client -T copy-mode-vi-quote-n
bind-key -T copy-mode-vi-quote-n y send-keys -X pipe "tmux loadb -b n -"
bind-key -T quote n switch-client -T quote-n
bind-key -T quote-n y paste-buffer -p -b n

bind-key -T copy-mode-vi-quote o switch-client -T copy-mode-vi-quote-o
bind-key -T copy-mode-vi-quote-o y send-keys -X pipe "tmux loadb -b o -"
bind-key -T quote o switch-client -T quote-o
bind-key -T quote-o y paste-buffer -p -b o

bind-key -T copy-mode-vi-quote p switch-client -T copy-mode-vi-quote-p
bind-key -T copy-mode-vi-quote-p y send-keys -X pipe "tmux loadb -b p -"
bind-key -T quote p switch-client -T quote-p
bind-key -T quote-p y paste-buffer -p -b p

bind-key -T copy-mode-vi-quote q switch-client -T copy-mode-vi-quote-q
bind-key -T copy-mode-vi-quote-q y send-keys -X pipe "tmux loadb -b q -"
bind-key -T quote q switch-client -T quote-q
bind-key -T quote-q y paste-buffer -p -b q

bind-key -T copy-mode-vi-quote r switch-client -T copy-mode-vi-quote-r
bind-key -T copy-mode-vi-quote-r y send-keys -X pipe "tmux loadb -b r -"
bind-key -T quote r switch-client -T quote-r
bind-key -T quote-r y paste-buffer -p -b r

bind-key -T copy-mode-vi-quote s switch-client -T copy-mode-vi-quote-s
bind-key -T copy-mode-vi-quote-s y send-keys -X pipe "tmux loadb -b s -"
bind-key -T quote s switch-client -T quote-s
bind-key -T quote-s y paste-buffer -p -b s

bind-key -T copy-mode-vi-quote t switch-client -T copy-mode-vi-quote-t
bind-key -T copy-mode-vi-quote-t y send-keys -X pipe "tmux loadb -b t -"
bind-key -T quote t switch-client -T quote-t
bind-key -T quote-t y paste-buffer -p -b t

bind-key -T copy-mode-vi-quote u switch-client -T copy-mode-vi-quote-u
bind-key -T copy-mode-vi-quote-u y send-keys -X pipe "tmux loadb -b u -"
bind-key -T quote u switch-client -T quote-u
bind-key -T quote-u y paste-buffer -p -b u

bind-key -T copy-mode-vi-quote v switch-client -T copy-mode-vi-quote-v
bind-key -T copy-mode-vi-quote-v y send-keys -X pipe "tmux loadb -b v -"
bind-key -T quote v switch-client -T quote-v
bind-key -T quote-v y paste-buffer -p -b v

bind-key -T copy-mode-vi-quote w switch-client -T copy-mode-vi-quote-w
bind-key -T copy-mode-vi-quote-w y send-keys -X pipe "tmux loadb -b w -"
bind-key -T quote w switch-client -T quote-w
bind-key -T quote-w y paste-buffer -p -b w

bind-key -T copy-mode-vi-quote x switch-client -T copy-mode-vi-quote-x
bind-key -T copy-mode-vi-quote-x y send-keys -X pipe "tmux loadb -b x -"
bind-key -T quote x switch-client -T quote-x
bind-key -T quote-x y paste-buffer -p -b x

bind-key -T copy-mode-vi-quote y switch-client -T copy-mode-vi-quote-y
bind-key -T copy-mode-vi-quote-y y send-keys -X pipe "tmux loadb -b y -"
bind-key -T quote y switch-client -T quote-y
bind-key -T quote-y y paste-buffer -p -b y

bind-key -T copy-mode-vi-quote z switch-client -T copy-mode-vi-quote-z
bind-key -T copy-mode-vi-quote-z y send-keys -X pipe "tmux loadb -b z -"
bind-key -T quote z switch-client -T quote-z
bind-key -T quote-z y paste-buffer -p -b z

