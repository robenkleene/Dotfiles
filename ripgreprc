# Having a column, e.g, with `--vimgrep` or `--column` interferes with `rg` in
# `consult-ripgrep` (the highlight for the current match in the file buffer will
# be off)
--vimgrep
# `--vimgrep` is roughly the same as below
# --column
# --no-heading

--smart-case
# Clear the background
# --colors=match:none
# --colors=match:fg:cyan
# --colors=path:fg:cyan
# --colors=line:fg:08
# --colors=column:fg:08
