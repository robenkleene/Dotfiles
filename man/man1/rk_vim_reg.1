.\" Automatically generated by Pandoc 3.9
.\"
.TH "RK_VIM_REG Reference" "" "" ""
.IP \(bu 2
\f[CR]let \(at*=\(ata\f[R]: Copy register \f[CR]a\f[R] to the system
clipboard
.SH Linewise
If a register contains a new line, it will be treated as linewise.
If it\(cqs linewise if it\(cqs appended to (e.g., \f[CR]\(dqAy\f[R])
then a new line will be appended between each yank.
.IP \(bu 2
Use \f[CR]:pu[t] <register>\f[R] to force paste linewise from a
register, to use the expression register, put the entire expression on
the line, e.g., \f[CR]:put =system(\(aqp\(aq)\f[R]
.IP \(bu 2
\f[CR]let \(atb=\(dq\(rsn\(dq\f[R]: Set a register just a line break (so
subsequent appends with \f[CR]\(dqBy\f[R] will have line break inserted
between them)
