.\" Automatically generated by Pandoc 3.8.3
.\"
.TH "RK_COMMENT_STYLE Reference" "" "" ""
.IP \(bu 2
Comments should explicitly reference the specific syntax they explain,
not just describe behavior abstractly
.IP \(bu 2
Include the relevant symbol, keyword, or construct directly in the
comment so the reader knows exactly what it refers to
.IP \(bu 2
Wrap referenced code in backticks (e.g., \f[CR]<code>\f[R]) in the
comment
.SS Examples
Good:
.IP
.EX
\f[I]# \(ga${[\(at]+}\(ga syntax avoids empty array being unbound under \(gaset \-u\(ga\f[R]
\f[B]for\f[R] part \f[B]in\f[R] \(dq${dir_parts[\(at]+\(dq${dir_parts[\(at]}\(dq}\(dq\f[B];\f[R] \f[B]do\f[R]
.EE
.IP
.EX
\f[I]# \(ga${parts[\-1]}\(ga not supported in older bash, use \(ga${#parts[\(at]}\-1\(ga instead\f[R]
last=\(dq${parts[${#parts[\(at]}\-1]}\(dq
.EE
.IP
.EX
\f[I]# \(ga__slots__\(ga prevents dynamic attribute creation, reducing memory usage\f[R]
__slots__ = (\(aqx\(aq, \(aqy\(aq)
.EE
.PP
Bad:
.IP
.EX
\f[I]# Avoid unbound variable error\f[R]
\f[B]for\f[R] part \f[B]in\f[R] \(dq${dir_parts[\(at]+\(dq${dir_parts[\(at]}\(dq}\(dq\f[B];\f[R] \f[B]do\f[R]
.EE
.IP
.EX
\f[I]# Negative indices not supported in older bash\f[R]
last=\(dq${parts[${#parts[\(at]}\-1]}\(dq
.EE
.IP
.EX
\f[I]# Reduces memory usage\f[R]
__slots__ = (\(aqx\(aq, \(aqy\(aq)
.EE
