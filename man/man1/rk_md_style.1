.\" Automatically generated by Pandoc 3.8.3
.\"
.TH "RK_MD_STYLE Reference" "" "" ""
.IP \(bu 2
Use real headers (\f[CR]## Foo\f[R]) instead of bold\-as\-header
(\f[CR]**Foo:**\f[R])
.IP \(bu 2
Use headers to introduce lists instead of indented hierarchical lists.
E.g., don\(cqt do \f[CR]1. **Foo**\f[R] followed by indented sub\-items;
instead use a header (\f[CR]### Foo\f[R]) followed by a flat list.
.SH Code Pointer Format
When referencing code in Markdown files, use a two\-block format: a
\f[CR]grep\f[R] fenced code block with the file path in
\f[CR]file:line:col:\f[R] format, followed by a language\-specific
fenced code block with the code snippet.
If no code block follows, use just the file path without the
\f[CR]grep\f[R] block (e.g.,
\f[CR]\(ti/Developer/Scratch/C/hello_world.c\f[R]).
.IP
.EX
\(ga\(ga\(ga grep
\(ti/Developer/Scratch/C/hello_world.c:3:1:
\(ga\(ga\(ga

\(ga\(ga\(ga c
int main() {
    printf(\(dqhello, world\(rsn\(dq);
}
\(ga\(ga\(ga
.EE
.SS Code Pointer Rules
.IP \(bu 2
Every file reference must use the code pointer format.
Never reference a file as bare text, inline code, or in a table without
a proper code pointer link.
.IP \(bu 2
When referencing a specific location in a file (e.g., a class, function,
or line), always include the exact line number.
Never reference a specific part of a file without a line number.
.IP \(bu 2
Never put \f[CR]\(aqs\f[R] after inline code.
E.g., write \f[CR]foo\f[R] \f[CR]bar:\f[R] instead of
\f[CR]foo\f[R]\(cqs \f[CR]bar:\f[R].
The possessive creates rendering conversion issues.
