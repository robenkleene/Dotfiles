.\" Automatically generated by Pandoc 3.9
.\"
.TH "RK_CPP_VECTOR Reference" "" "" ""
.IP
.EX
#include <vector>
#include <iostream>
int main() {
  std::vector<int> vec = {1, 2, 3, 4, 5};
  std::cout << \(dqNumber of elements in vector: \(dq << vec.size() << std::endl;
  return 0;
}
.EE
.PP
\f[CR]emplace_back\f[R] vs \f[CR]push_back\f[R]:
.IP
.EX
#include <vector>
#include <iostream>
#include <string>
struct Widget {
  std::string name;
  Widget(const std::string& n) : name(n) {
      std::cout << \(dqConstructed Widget: \(dq << name << std::endl;
  }
  Widget(const Widget& other) : name(other.name) {
      std::cout << \(dqCopied Widget: \(dq << name << std::endl;
  }
  Widget(Widget&& other) noexcept : name(std::move(other.name)) {
      std::cout << \(dqMoved Widget: \(dq << name << std::endl;
  }
};
int main() {
  std::vector<Widget> widgets;
  Widget w1(\(dqpush\(dq);
  widgets.push_back(w1); // Calls copy constructor
  widgets.push_back(Widget(\(dqpush_temp\(dq)); // Calls move constructor
  widgets.emplace_back(\(dqemplace\(dq); // Constructs in place, no copy/move
  return 0;
}
.EE
