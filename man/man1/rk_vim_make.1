.\" Automatically generated by Pandoc 3.6.3
.\"
.TH "RK_VIM_MAKE Reference" "" "" ""
.IP \[bu] 2
\f[CR]:make\f[R] always runs the \f[CR]makeprg\f[R] relative to the
current working directory \f[I]and that can\[cq]t be changed after
running\f[R], so if \f[CR]:make\f[R] outputs paths for the
\f[CR]quickfix\f[R] list, then \f[CR]:make\f[R] has to be run in the
working directory those paths are relative to)
.SH \f[CR]makeprg\f[R]
.IP
.EX
set makeprg=gcc\[rs] %
.EE
.PP
Or
.IP
.EX
let &makeprg = \[aq]gcc %\[aq]
.EE
.SS Pipes
Pipes need to be escaped.
.IP
.EX
set makeprg=echo\[rs] \[rs]\[dq]Hello\[rs] World\[rs]\[dq]\[rs] \[rs]\[rs]\[rs]|\[rs] pbcopy\[aq]
.EE
.IP
.EX
let &makeprg = \[aq]echo \[dq]Hello World\[dq] \[rs]| pbcopy\[aq]
.EE
