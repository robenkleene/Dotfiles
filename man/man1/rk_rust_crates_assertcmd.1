.\" Automatically generated by Pandoc 3.8.3
.\"
.TH "RK_RUST_CRATES_ASSERTCMD Reference" "" "" ""
.PP
To get the output of a test, with:
.IP
.EX
        let output = mov()
            .current_dir(tmp_dir_path)
            .write_stdin(input)
            .args(&[\(dqchange\(dq, \(dqaltered\(dq, \(dq\-w\(dq])
            .assert()
            .success().get_output();
.EE
.PP
To get both \f[CR]STDIN\f[R] and \f[CR]STDOUT\f[R] of a test:
.IP
.EX
        let command = mov()
            .current_dir(tmp_dir_path)
            .write_stdin(input)
            .args(&[\(dqchange\(dq, \(dqaltered\(dq, \(dq\-w\(dq])
            .assert()
            .success();
        let output = command.get_output();
        println!(\(dqstdout = {:?}\(dq, String::from_utf8_lossy(&output.stdout));
        println!(\(dqstderr = {:?}\(dq, String::from_utf8_lossy(&output.stderr));
.EE
