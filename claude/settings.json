{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "t=$PPID; while [ -n \"$t\" ] && [ \"$t\" -gt 1 ]; do d=$(ps -o tty= -p \"$t\" 2>/dev/null | tr -d \" \"); case \"$d\" in tty*|pts*) printf \"\\a\" > \"/dev/$d\" 2>/dev/null; break;; esac; t=$(ps -o ppid= -p \"$t\" 2>/dev/null | tr -d \" \"); done"
          }
        ]
      }
    ]
  },
  "tui": "fullscreen",
  "preferredNotifChannel": "terminal_bell",
  "showTips": false
}
