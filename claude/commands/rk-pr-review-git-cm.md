---
name: rk-pr-review-git-cm
description: prompt review git commit
user-invocable: true
---

Code review the current commit (check the current commit with `git rev-parse HEAD` [don't switch commits if this isn't what you expect]).
