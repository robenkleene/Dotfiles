;;; robenkleene-ediff.el --- robenkleene-ediff
;;; Commentary:
;;; Code:

(setq ediff-split-window-function 'split-window-horizontally)

;; Don't use that separate frame for control panel
;; (setq ediff-window-setup-function 'ediff-setup-windows-plain)

(provide 'robenkleene-ediff)
;; Local Variables:
;; byte-compile-warnings: (not free-vars)
;; End:
;;; robenkleene-ediff.el ends here
