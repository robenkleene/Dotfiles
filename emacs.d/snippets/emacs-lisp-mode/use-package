# -*- mode: snippet -*-
# name: use-package
# key: use-package
# --
(eval-when-compile
  (unless (bound-and-true-p package--initialized)
    (package-initialize))
  (require 'use-package))
(use-package ${1:my-package})