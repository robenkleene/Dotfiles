;; eshell aliases (comment example)
;; Trick to get `git' to use it's default color behavior, this works because it
;; overrides the `TERM=dumb' and then `git' it's default behavior of only
;; applying color if it's not being piped
;; `TERM=eterm-color' might be slightly more correct?
alias git TERM= git $*
alias rg TERM= rg $*
;; alias fd fd --color=never $*
;; Mercurial needs `xterm-256color` or similar, because it uses more colors by default
;; alias hg TERM=xterm-256color hg --pager="" $*
;; alias - cd -

alias pull egit -u && ~/Developer/Dotfiles/install/update.sh
