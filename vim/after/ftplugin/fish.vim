" This makes traversing paths easier
set iskeyword-=.
set iskeyword-=/

" Set formatexpr to default to wrap comments
set formatexpr&
" Disable the `omnifunc` because it's causing errors
" set omnifunc&
" If I'm still having completion errors, try running `CocUninstall coc-fish`
