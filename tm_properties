PATH = "$PATH:~/.bin"
# rbenv
TM_RUBY = "$HOME/.rbenv/shims/ruby"
PATH = "$HOME/.rbenv/bin:$PATH"
# npm
PATH = "/usr/local/share/npm/bin:$PATH"
NODE_PATH = "/usr/local/share/npm/lib/node_modules:/usr/local/lib/node_modules:$NODE_PATH"

# Exclude from Project Folder
excludeDirectories = "{$excludeDirectories,node_modules}"
exclude = "{$exclude,tags}"

[ "{zshrc,vimrc}" ] 
fileType = "source.tm-properties" 

[ Fastlane ] 
fileType = "source.ruby" 

[ *.coffee ]
tabSize = 2  
softTabs = true

[ *.js ]
tabSize = 2  
softTabs = true
